<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ViewModels="clr-namespace:App_X.ViewModels"
             mc:Ignorable="d"
             x:Class="App_X.Views.AddUserView">
    <ContentPage.BindingContext>
        <ViewModels:AddUserVM />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="ชื่อ" Text="{Binding User.User_Name, TargetNullValue=''}" />
            <Entry Placeholder="อีเมลล์" Text="{Binding User.ID_User, TargetNullValue=''}" />
            <Entry Placeholder="รหัสผ่าน" Text="{Binding User.User_Pass, TargetNullValue=''}" />
            <Entry Placeholder="เบอร์มือถือ" Text="{Binding User.User_Tell, TargetNullValue=''}" />
            <Entry Placeholder="ส่วนสูง" Text="{Binding User.User_Height, TargetNullValue=''}" />
            <Entry Placeholder="น้ำหนัก" Text="{Binding User.User_Weight, TargetNullValue=''}" />
            <Entry Placeholder="รหัสพิเศษ (ไม่มีไม่ต้องกรอก)" Text="{Binding User.User_Type, TargetNullValue=''}" />

            <Button Command="{Binding AddCommand}" Text="สมัครสมาชิก" />
   
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
