@model IEnumerable<WebFit.Models.Table_TypePG>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table id="example1" class="table table-bordered table-striped" style="width:100%">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypePG_Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypePG_Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID_TypePG }) |
                @Html.ActionLink("Details", "Details", new { id = item.ID_TypePG }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ID_TypePG })
            </td>
        </tr>
    }

</table>
